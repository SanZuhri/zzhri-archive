---
title: 'How to Add a Post'
pubDate: 2025-10-30
---

import FlowDiagram from '@/components/examples/FlowDiagram.astro'

export const postSteps = [
  {
    number: 1,
    title: 'Run CLI',
    description: 'pnpm new:post <collection> "Title"'
  },
  {
    number: 2,
    title: 'Done!',
    description: 'Post created with frontmatter'
  },
  {
    number: 3,
    title: 'Edit Content',
    description: 'Open file and write your content'
  },
  {
    number: 4,
    title: 'Add Images',
    description: 'Put images in _assets/ folder (optional)'
  },
  {
    number: 5,
    title: 'Preview',
    description: 'Save and refresh browser (hot reload!)'
  }
]

Want to add a post? Here's how.

## Quick Start Flow

<FlowDiagram steps={postSteps} title="Post Creation Workflow" />

---

## The fast way (CLI)

Just run this:

```bash
pnpm new:post thoughts "Life Lessons from 2024"
```

Done. The CLI creates the file, adds the frontmatter, generates a proper slug, and tells you where everything is:

```
✨ Post created successfully!

📄 File: src/content/thoughts/life-lessons-from-2024.md
📅 Date: 2024-11-01
🔗 URL: /collections/thoughts/life-lessons-from-2024/
```

### Don't remember the collection name?

No problem. Just run `pnpm new:post` without arguments and it'll ask you:

```
📝 Create a new post

Available collections:
  1. posts
  2. thoughts
  3. media
  4. gallery
  5. projects

Collection name: thoughts
Post title: Life Lessons from 2024
Add image? (y/N): n
```

### Got images?

The CLI handles that too:

```bash
pnpm new:post gallery "Sunset Photos"
# Add image? y
# Image filename: sunset.jpg
```

It'll create the post with the image reference and remind you to add the actual file to `_assets/`.

---

## 📝 Method 2: Manual Setup

If you prefer manual control, follow these steps:

### Step 1: Choose Your Collection

Pick the collection that fits your content:

| Collection | Folder | Purpose | URL Pattern |
|------------|--------|---------|-------------|
| **Thoughts** | `src/content/thoughts/` | Reflections & ideas | `/collections/thoughts/{slug}` |
| **Media** | `src/content/media/` | Books, movies, music | `/collections/media/{slug}` |
| **Gallery** | `src/content/gallery/` | Photos & visuals | `/collections/gallery/{slug}` |
| **Projects** | `src/content/projects/` | Work & creativity | `/collections/projects/{slug}` |
| **Posts** | `src/content/posts/` | Guides & docs | `/{slug}` |

**Tip:** Check `src/collections.config.ts` to see all available collections and their settings.

### Step 2: Create the File

Create a new file in your chosen collection folder:

**Examples:**

For a thought/reflection:
```bash
src/content/thoughts/life-lessons.md
```

For a media review:
```bash
src/content/media/favorite-book.md
```

For a gallery post:
```bash
src/content/gallery/sunset-photos.md
```

For a project:
```bash
src/content/projects/portfolio-website.md
```

**Naming Rules:**
- ✅ Use lowercase letters
- ✅ Use hyphens for spaces (`my-post` not `my_post` or `myPost`)
- ✅ Be descriptive (`chocolate-cake` not `post1`)
- ❌ Avoid special characters (`!@#$%`)
- 💡 File name becomes the URL slug

### Step 3: Add Frontmatter

Every post needs frontmatter at the top:

```markdown
---
title: 'Life Lessons from 2024'
pubDate: 2024-11-01
image: '/images/lessons.jpg'  # Optional
---
```

**Required Fields:**
- `title` - The post title (string)
- `pubDate` - Publication date (YYYY-MM-DD format)

**Optional Fields:**
- `image` - Featured image path (for OG images and thumbnails)

### Step 4: Write Your Content

After the frontmatter, write your content using Markdown:

```markdown
---
title: 'Life Lessons from 2024'
pubDate: 2024-11-01
---

This year has taught me valuable lessons...

## Lesson 1: Patience

Learning to be patient with myself...

## Lesson 2: Growth

Embracing change and personal growth...
```

### Step 5: Preview Your Post

**If dev server is already running:**
- Just save your file
- Refresh your browser
- Changes appear automatically! ✨

**If dev server is not running:**

```bash
npm run dev
```

Then open your browser and visit:
- For collection posts: `http://localhost:4321/collections/{collection}/{slug}/`
- For guide posts: `http://localhost:4321/{slug}/`

**Example URLs:**
- `http://localhost:4321/collections/thoughts/life-lessons/`
- `http://localhost:4321/collections/media/favorite-book/`
- `http://localhost:4321/how-to-add-post/` (guide post)

**Hot Reload:**
Posts in existing collections support hot reload - no need to restart the dev server!

## Markdown Features

Archive supports standard Markdown plus:

### Headings
```markdown
# H1 Heading
## H2 Heading
### H3 Heading
```

### Lists
```markdown
- Unordered item
- Another item

1. Ordered item
2. Another item
```

### Links
```markdown
[Link text](https://example.com)
```

### Images

**Option 1: Using _assets folder (Recommended)**
```markdown
![Alt text](./\_assets/my-image.jpg)
```

**Option 2: Using public folder**
```markdown
![Alt text](/images/my-image.jpg)
```

### Code Blocks
````markdown
```javascript
const hello = "world"
```
````

### Blockquotes
```markdown
> This is a quote
```

## Adding Images to Posts

Each collection has an `_assets/` folder for storing images. This keeps your content organized.

### Folder Structure

```
src/content/thoughts/
├── _assets/
│   ├── photo1.jpg
│   └── photo2.png
├── my-post.md
└── another-post.md
```

### Using Images in Posts

**Step 1:** Add your image to the `_assets/` folder

```bash
src/content/thoughts/_assets/sunset.jpg
```

**Step 2:** Reference it in your post using relative path

```markdown
---
title: 'Beautiful Sunset'
pubDate: 2024-11-01
---

![Sunset at the beach](./\_assets/sunset.jpg)

This sunset was absolutely stunning...
```

### Image Path Options

| Method | Path | Use Case |
|--------|------|----------|
| **_assets (Recommended)** | `./\_assets/image.jpg` | Collection-specific images |
| **Public folder** | `/images/image.jpg` | Shared images across site |
| **External URL** | `https://...` | Images hosted elsewhere |

### Why Use _assets?

- ✅ **Organized** - Images stay with their posts
- ✅ **Portable** - Move collection folder, images come with it
- ✅ **Clear** - Easy to see which images belong to which posts
- ✅ **Clean** - No clutter in public folder

## Draft Posts

To create a draft post that won't appear on your site, prefix the filename with an underscore:

**Examples:**
```bash
src/content/thoughts/_work-in-progress.md
src/content/media/_draft-review.md
src/content/gallery/_example.md
```

**How it works:**
- ✅ Files starting with `_` are ignored during build
- ✅ They won't appear in post lists or be accessible via URL
- ✅ Perfect for work-in-progress content
- ✅ Remove the `_` prefix when ready to publish

## Tips for Great Posts

1. **Use descriptive titles** - Make it clear what the post is about
2. **Add headings** - Break up content with H2 and H3 headings
3. **Include images** - Visual content enhances readability
4. **Keep it focused** - One topic per post
5. **Proofread** - Check for typos and formatting issues

## Post URLs

Posts in different collections have different URL patterns:

- **Guide posts** (`posts` collection): `/{slug}`
  - Example: `/how-to-add-post/`
  
- **Collection posts**: `/collections/{collection}/{slug}`
  - Example: `/collections/thoughts/life-lessons/`
  - Example: `/collections/media/favorite-book/`

## Development Workflow

### Hot Reload (No Restart Needed)

When working on posts, you get instant feedback:

1. **Start dev server once:**
   ```bash
   npm run dev
   ```

2. **Edit your posts:**
   - Save file
   - Refresh browser
   - See changes instantly!

3. **What works with hot reload:**
   - ✅ Content changes
   - ✅ Frontmatter updates
   - ✅ New posts in existing collections
   - ✅ Image additions
   - ✅ Component edits

### When to Restart

Only restart if you:
- ❌ Add a new collection
- ❌ Edit config files
- ❌ Install new packages

**Quick restart:**
```bash
# Stop server (Ctrl+C), then:
npm run dev
```

## Next Steps

Now that you know how to add posts, explore:
- [How to Add a New Collection](/how-to-add-collection/) - Create custom collections
- [Customizing Your Archive](/customizing-archive/) - Personalize your archive
- [Using MDX](/using-mdx/) - Add interactive components to your posts

Happy writing! ✍️
